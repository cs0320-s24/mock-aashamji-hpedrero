
// Just an example function in a code module, so we can demo 
// testing arbitrary TypeScript functions outside React
/**
 * 
 * @returns *
 * @returns {number} The constant value 0.
 */
 
export function zero() {
    return 0
}